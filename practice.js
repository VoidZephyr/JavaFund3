// const updateAge = (object, age) => {
//     object.age = age
//     return object
//   }
//   const updateAge = (object, age) => {
//     return { ...object, age };
//   };
  

//----------------------------
//   Given the following function signature, use what you've learned to allow printArgs() to return an array with all individual arguments that were passed to the function (an undefined number).

//let printArgs = (/**/) => {
//     //YOUR CODE HERE
//   };

// let printArgs = (...args) => {
//     return args;
//   };
//   console.log(printArgs(1, 2, 3, 4));
//------------------------------------

// const address = (addressParts) => (
//     `${addressParts.street}, ${addressParts.city}, ${addressParts.state} ${addressParts.zip}`
// );

// const address = ({ street, city, state, zip }) => (
//     `${street}, ${city}, ${state} ${zip}`
//   );


  //--------------------------------

  

